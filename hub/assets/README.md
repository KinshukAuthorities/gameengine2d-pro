# Hub visual assets

`hub_background.png` is the full-window visual backdrop used by `hub.exe`.
It is an engine-owned original Abyss atmosphere illustration, deliberately
darkened and overlaid in the Hub so it works as product ambience rather than
as a project-specific game screen. It is packaged next to the executable by
the Hub CMake target.
