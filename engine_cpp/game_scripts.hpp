#pragma once
// Neutral compatibility header for native scripts.
// Project scripts are compiled through the generated build header,
// not through engine core includes.

#include "script_system.hpp"
